# Strategy 377 — ESLint compatibility

Independent base: main bff19fd1871543a58f556bb4f7635384b6d9ad4c in mintpixels/grillers-medusa-frontend.

Pinned ESLint 8.57.1 alongside the existing eslint-config-next / Next plugin 15.3.9. Updated the npm and Yarn locks. A new worktree completed the documented Yarn installation; the resulting lock also passed `yarn install --frozen-lockfile --ignore-scripts --non-interactive`. Unrelated existing Yarn resolutions are retained.

`npm run lint` now loads the Next rules and reaches source linting. It does not pass: pre-existing source errors affect 16 files. The protected launch-chain files with errors are profile-sms-marketing-page.test.tsx, arrival-calendar, fulfillment-step, payment, shipping, and staff/phone-order-copilot. Errors include internal links, unescaped punctuation, and references to unregistered TypeScript rules.

No lint rule was disabled. `eslint.ignoreDuringBuilds` remains true as on the base: changing build enforcement would introduce a separate release gate while the source errors remain. The normal standalone lint command still fails visibly. CI continues to cover Jest, TypeScript and the build; green CI would not prove lint acceptance.

This is a compatibility repair with a documented source-cleanup blocker. It cannot close #377 while the requested launch-chain ownership boundary prevents repairing all lint errors. Operations-guide update is unnecessary because staff/customer behavior is unchanged.

Issue: https://github.com/aviswerdlow/grillers-pride-strategy/issues/377
